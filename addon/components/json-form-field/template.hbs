{{yield (hash
    validation=validation
    self=this
    showMessage=showMessage
    component=(component fieldComponent
      value=(readonly (get form.iniData formPath))
      isGroup=isGroup
      formPath=formPath
      form=form
      field=field
      onChange=(action 'onChange')))
}}